
import 'package:flutter/material.dart';

class LiveMatchScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context){

    return Scaffold(
      appBar: AppBar(title: Text("Live Match")),
      body: Center(child: Text("Live Score Updates")),
    );

  }

}
